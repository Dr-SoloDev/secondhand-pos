# Scale Agent env — Tiger TI-01 RS232
# ก๊อปเป็น scale-agent.env แล้วแก้ค่า

# Serial port — เว้นว่าง = auto-scan COM1-COM10
SCALE_PORT=
# Baud rate
SCALE_BAUD=9600
# Model name
SCALE_MODEL=Tiger TI-01
# HTTP
SCALE_AGENT_HOST=0.0.0.0
SCALE_AGENT_PORT=9130
