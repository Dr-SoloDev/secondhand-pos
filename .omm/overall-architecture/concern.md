1. CRITICAL: ยังไม่มี scale integration (digital weighing scale) — คู่แข่งมีหมด
2. HIGH: ยังไม่มี offline mode (PWA)
3. G1 (Photo Upload) รอ owner decision — blocked
4. Cloudflare Tunnel ยังไม่ setup — remote access ไม่ได้
